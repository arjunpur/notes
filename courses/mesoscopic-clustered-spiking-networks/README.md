# Mesoscopic Theory of Clustered Spiking Networks

Course slug: `mesoscopic-clustered-spiking-networks`

Canonical PDF:

```bash
courses/mesoscopic-clustered-spiking-networks/mesoscopic-clustered-spiking-networks-lecture-notes.pdf
```

Build from this directory:

```bash
make all
```

The course uses the shared `coursenotes` compatibility scaffold copied from `courses/models-of-neural-systems`. Generated raster figures live only in `figures/`; figure prompts and status are recorded in `figures/figure-metadata.md`.

Open citation and figure limitations are tracked in `backmatter/citation-and-figure-todos.tex` and in the coordinator `final-report.md`.
